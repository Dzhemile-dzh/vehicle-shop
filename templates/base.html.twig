<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Vehicle Shop{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}{% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="text-white" style="background: radial-gradient(102.21% 102.21% at 50% 28.75%, #00121C 42.62%, #013954 100%);">
        <div class="flex flex-col justify-between relative">
            <div>
                <header class="h-[114px] shrink-0 flex flex-col sm:flex-row items-center sm:justify-between py-4 sm:py-0 px-6 border-b border-white/20 shadow-md">
                    <a href="/">
                        <img class="h-[42px]" src="{{ asset('images/carshop-logo.png') }}" alt="carshop logo">
                    </a>
                    <nav class="flex space-x-4 font-semibold">
                        <a class="hover:text-amber-400 pt-2" href="{{ path('car_list') }}">
                            Cars
                        </a>
                        <a class="hover:text-amber-400 pt-2" href="{{ path('motorcycle_list') }}">
                            Motorcycles
                        </a>
                        <a class="hover:text-amber-400 pt-2" href="{{ path('truck_list') }}">
                            Trucks
                        </a>
                        <a class="hover:text-amber-400 pt-2" href="{{ path('trailer_list') }}">
                            Trailers
                        </a>

                        {% if app.user %}
                            <a class="rounded-[60px] py-2 px-5 bg-white/10 hover:bg-white/20" href="{{ path('app_logout') }}">
                                Logout
                            </a>
                            <div class="border-l border-white/2 pl-2 py-2 px-5" >
                                <span >Welcome, {{ app.user.firstName }}</span>
                            </div>
                        {% else %}
                            <a class="rounded-[60px] py-2 px-5 bg-white/10 hover:bg-white/20" href="{{ path('app_login') }}">
                                Login
                            </a>
                        {% endif %}
                        </div>
                    </nav>
                </header>
                {% block body %}{% endblock %}
            </div>
            <div class="p-5 bg-white/5 mt-3 text-center">
                Made with ❤️ by <a class="text-[#0086C4]">Dzhemile Ahmed</a>
            </div>
        </div>
    </body>
</html>
